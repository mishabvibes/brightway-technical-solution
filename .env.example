# The public address of the live website, with no trailing slash.
# Used for canonical URLs, the sitemap, social previews, structured data and llms.txt.
# Set this in your hosting dashboard (e.g. Vercel > Settings > Environment Variables) BEFORE building.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
